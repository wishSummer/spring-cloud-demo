spring:
  application:
    name: provider
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
    sentinel:
      transport:
        # 控制台地址
        dashboard: 127.0.0.1:8718